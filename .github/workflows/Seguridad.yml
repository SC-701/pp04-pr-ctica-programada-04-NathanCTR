name: Deploy Seguridad API to Azure

env:
  AZURE_WEBAPP_NAME:         APISeguridadJohn
  AZURE_WEBAPP_PACKAGE_PATH: 'Vehiculo.API/API'
  DOTNET_VERSION:            '8.0'

on:
  push:
    branches: [ "main" ]
    paths:
      - 'Vehiculo.API/API/**'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: 'BD'

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Configurar NuGet feed privado
        run: |
          dotnet nuget add source \
            "https://nuget.pkg.github.com/SC-701/index.json" \
            --name "Paquetes" \
            --username "${{ github.actor }}" \
            --password "${{ secrets.PaqueteTOKEN }}" \
            --store-password-in-clear-text

      - name: Restaurar dependencias
        run: |
          cd "Vehiculo.API"
          dotnet restore "Vehiculo.API/Abstracciones/Abstracciones.csproj"
          dotnet restore "Vehiculo.API/DA/DA.csproj"
          dotnet restore "Vehiculo.API/Flujo/Flujo.csproj"
          dotnet restore "Vehiculo.API/Reglas/Reglas.csproj"
          dotnet restore "Vehiculo.API/API/API.csproj"

      - name: Compilar
        run: |
          cd "Vehiculo.API"
          dotnet build "API/API.csproj" --configuration Release --no-restore

      - name: Publicar (dotnet publish)
        run: |
          cd "Vehiculo.API"
          dotnet publish "API/API.csproj" \
            --configuration Release \
            --no-build \
            --output ./publish

      - name: Deploy a Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name:        ${{ env.AZURE_WEBAPP_NAME }}
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE_SEGURIDAD }}
          package:         './Vehiculo.API/publish'
